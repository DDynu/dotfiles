windowrule = float on, match:title (float)

# Floating classes for apps
windowrule = float on, match:title (iwgtk)|(Bluetooth)|(Calendar)|(Pipewire Volume Control)
windowrule = size 1200 850, match:title (iwgtk)|(Bluetooth)|(Calendar)
windowrule = float on, match:class (iwgtk)|(blueman-manager)

# Steam
windowrule = float on, size 1200 850, center on, match:title (Friends List)|(Steam Settings)
windowrule = float off, match:title (Steam)
windowrule = match:title (?i).*(nomachine).*, fullscreen on

# Looking glass
windowrule = fullscreen on, match:class (looking-glass-client)
